      SUBROUTINE TAKON (T,V,C,NMAT)
C
C
C
      IMPLICIT INTEGER (A-Z)
C
C
      REAL*8 T(NMAT),V(NMAT),C(NMAT)
      COMMON /T102/ NOUT,HALF,THIRD,MAXNO,OUTBUF,KINADR
      COMMON /CORE/ NCORE
      COMMON /TAPES/ NIN,IR,IOUT,ICHECK,IFLAG3,ITAPE,I30
      CALL RGETSA(NOUT,KINADR)
      CALL SWRIT(NOUT,T,INTOWP(NMAT))
      CALL SWRIT(NOUT,V,INTOWP(NMAT))
C     IF (NCORE.NE.0) CALL SWRIT(NOUT,C,INTOWP(NMAT))
CTJL  CALL SWEOF(NOUT)   NO NEED TO WRITE EOF ON IBM
C   LEFT HERE FOR LATER REFERENCE
      CALL RGETSA(NOUT,IADDR)
C
      WRITE (ICHECK,900) IADDR
  900 FORMAT (' FILE92 USED A TOTAL OF ',I8,' SECTORS')
CRBR  IF (IFLAG3.GT.0) WRITE (IFLAG3,900) IADDR
C
      RETURN
      END
